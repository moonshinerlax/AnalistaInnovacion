{
  "version": 2,
  "builds": [
    {
      "src": "docs/browser",
      "use": "@vercel/static-build",
      "config": { "distDir": "docs/browser" }
    },
    {
      "src": "docs/server",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
